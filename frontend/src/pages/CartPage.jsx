export default function CartPage() {
  return (
    <div style={{ paddingTop: '70px' }}>
      <section className="section container">
        <h1>Carrito</h1>
        <p className="text-muted" style={{ marginTop: '1rem' }}>
          Tu carrito de compras — próximamente.
        </p>
      </section>
    </div>
  )
}
